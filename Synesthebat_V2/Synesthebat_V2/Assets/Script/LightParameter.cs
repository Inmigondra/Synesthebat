using UnityEngine;
using System.Collections;

public class LightParameter : MonoBehaviour {
	/*FMOD.Studio.EventInstance soundInstance;
	FMOD.Studio.ParameterInstance Luminosite;
	public float lightValue;

	// Use this for initialization
	void Start () {
		
		soundInstance.getParameter ("Luminosite", out Luminosite);
		//Luminosite.getValue (out lightValue);
		Luminosite.setValue(20.0f);
		Debug.Log (Luminosite);
	}
	
	// Update is called once per frame
	void Update () {
		Debug.Log (Luminosite);
	}*/
}
